meta {
  name: Login
  type: http
  seq: 6
}

post {
  url: http://localhost:8080/api/users/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "admin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
