meta {
  name: Login Admin
  type: http
  seq: 8
}

post {
  url: http://localhost:8080/api/users/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "admin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
